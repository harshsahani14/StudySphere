import React from 'react'

const HighLightText = ({text}) => {
  return (
  
        <span className=' gradient-text font-[600] text-[36px] leading-[44px] text-left  '>
          {text}
        </span>

    
  )
}

export default HighLightText
